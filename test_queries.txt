
//Get number of items per category for line chart
SELECT category, count(*) FROM inventory
GROUP BY category;


//Admin view will show all projects
CREATE VIEW admin AS
SELECT project.proj_id, users.id, project.name, users.email
FROM project
INNER JOIN users ON users.id = project.manager_id
ORDER BY proj_id ASC;
